name: imdb
description: A sample project using the IMDB API

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.1.3
  http: ^0.13.5
  flutter_secure_storage: ^6.0.0
  logging: ^1.0.2
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0
  build_runner: ^2.3.0
  network_image_mock: ^2.0.0

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
